export interface User {
    id?: string,
    userName?: string,
    email?: string,
    firstName?: string,
    lastName?: string,
    access_token?: string,
    refresh_token?: string,
    roles?: string[]

}
